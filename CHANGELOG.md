# Changelog

本项目的用户可见变更记录。日期为北京时间。

## [v0.03.1] - 2026-09-03

### 修复

- **A 股个股行情停更**：v0.03 为数据源新增的超时限制（akshare 15 秒）低于 akshare 批量拉取的实际耗时，导致线上 A 股个股全部超时失败、行情停留在上一交易日收盘。线上配置已将 A 股个股行情源由 akshare 切换为 tencent（与 ETF / 港股 / 指数一致），数据恢复实时
- akshare 默认超时由 15 秒放宽至 45 秒（批量拉取实测需 20~40 秒；如需切回 akshare 数据源不再被误掐）
- `/api/admin/status` 后台任务时间偏差 8 小时：任务状态时间曾以 UTC 写入 SQLite（SQLite 不保存时区，读出后被按宿主机时区错标）。改为与库内其余时间列一致的北京时间落库，并对无时区值显式标注 +08:00

## [v0.03] - 2026-09-02

### 新增

#### 标签

- 股票 / ETF 自选支持标签（指数不支持）：先在标签管理页（`/tags`）创建标签，再到自选管理页用行内下拉关联，一个自选条目最多一个标签
- 标签支持新增、改名、删除与引用数量查看；名称去首尾空格、非空、不重复、限 50 字符
- 被股票 / ETF 引用的标签禁止删除（返回 409 与引用数量提示），业务层计数检查与数据库外键双层保护
- 行情首页支持按标签筛选（全部 / 指定标签 / 无标签）；筛选为前端本地过滤，不增加任何数据源请求
- 行情首页底部显示当前版本号（`StocksView v0.03`），`/health` 返回 `version` 字段

#### 数据库迁移（Alembic）

- 引入 Alembic 管理数据库结构：`0001_v002_baseline`（v0.02 基线）+ `0002_v003`（新增 tag / job_status 表、watchlist.tag_id 列）
- 容器启动自动执行 `alembic upgrade head`，成功后才启动应用；迁移失败容器退出，不再出现代码与数据库版本不一致
- **升级注意**：已有 v0.02 数据库需先执行 `alembic stamp 0001_v002_baseline` 打基线标记（详见 README「v0.02 → v0.03 升级」），升级后原有数据完整保留、已有自选默认无标签

#### 可观测性

- 后台任务状态持久化：`GET /api/admin/status` 可查看行情 / 估值刷新任务的最近开始 / 成功 / 失败时间、耗时与连续失败次数（失败不清除最近成功时间，成功清零连续失败）
- 数据源超时配置化（`providers.timeout`，tencent 8 / akshare 15 / tushare 15 秒，可调）：超时不再无限等待，该次调用按失败处理并保留最后一次成功行情
- 数据源运行指标：成功 / 报错 / 超时分别计数，含最近耗时与最近成功 / 失败时间，经 `GET /api/admin/status` 的 `providers` 节查看（进程内存指标，重启后重新计数）

### 内部

- 本地启动步骤增加 `alembic upgrade head`（取代自动建表）；pyproject 版本对齐应用版本体系

## [v0.02] - 2026-08-22

### 修复

#### 港股指数添加

- 恒生科技指数（`HSTECH`）等港股指数可正常添加：港股指数代码为字母缩写，此前用户无从得知应填什么（如误填 `HS2083` 报「无法识别证券」）
- 指数代码大小写不敏感：`hstech` 与 `HSTECH` 视为同一证券，不再产生大小写重复配置；代码首尾空白自动清理
- 港股指数识别失败时，报错信息附带常见指数代码指引（HSI 恒生指数 / HSCEI 国企指数 / HSTECH 恒生科技指数）
- 指数添加表单提示与 README 补充港股指数代码示例与代码表

#### 估值（PE(TTM) / PB / 股息率(TTM)）

- 修复核心缺陷：当日估值刷新时自选列表中只要有任意一只股票已取到数据，后续新添加的 A 股就永远不会被补抓（线上曾出现 10 只自选 A 股仅 1 只有估值）。现改为按自选 A 股覆盖率判定，盘中新增自选最迟 30 分钟内自动补齐
- 添加自选 A 股后立即获取并显示该股最近一期估值，不再需要等待收盘后的定时刷新
- 修复估值误写入上市初期历史数据的问题（此前会取到 2001 年的 PE/PB，现限定查询窗口并保留最新一期）
- 修复服务启动时因交易日历尚未就绪导致估值补刷被静默跳过的问题（旧版本线上能启动即刷属时序巧合）
- 应对数据源当日指标延迟生成：Tushare 当日股息率字段晚间才回填，指标不全的当日快照会自动周期重试直至补齐；指标当日确无值的标的（如亏损股 PE 为空）每 30 分钟重试一次全市场查询至当日结束
- 停牌、新股等当日无数据的标的在内存中标记已尝试，当日不再重复请求；`POST /api/admin/refresh/fundamentals` 手动刷新始终强制重试全部自选 A 股

## [v0.01] - 2026-08-19

- 初始版本：A 股 / 港股股票、ETF 与指数行情看板，A 股股票 PE(TTM)/PB/股息率(TTM) 估值
- 添加自选/指数后无论市场状态立即刷新一次行情
